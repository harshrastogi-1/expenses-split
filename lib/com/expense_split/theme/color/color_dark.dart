import 'package:flutter/material.dart';

class ColorDark {
  static const Color colorPrimary = Color(0xFF202026);
  static const Color white = Colors.white;
  static const Color colorPrimaryDark = Color(0xFF28282D);
  static const Color darkTabBackgroundColor =
      Color.fromRGBO(38, 29, 26, 1); // Darker shade of your original color
  static Color darkHoverColor = Colors.grey[800]!;
  static const Color darkTextColor = Color.fromRGBO(
      189, 155, 143, 1); // Lighter text for readability in dark mode
  static const Color primaryColor = Color.fromRGBO(
      189, 155, 143, 1); // A more muted, darker tone of original primary color
  static const Color darkContainerBackgroundColor =
      Color.fromRGBO(30, 30, 30, 1); // Darker background color for container
}
